/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Assurez-vous qu'il n'y a pas de redirections ou de rewrites qui pourraient interférer
}

module.exports = nextConfig